- WorkoutService сейчас использует ExerciseRepository для создания логов в completion flow. Это рабоче и целостно, но если мы захотим еще строже разнести границы модулей, потом можно вынести это в отдельный application service или фасад.